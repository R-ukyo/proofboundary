# 検証の仕組みと保証の根拠

この文書の目的は、「Verified Codeの実装diffを人間が読まなくてよい」と主張できる
**理由**を、検証の仕組みに即して説明することです。結論を先に書きます。

> 人間が読まなくてよいのは **Tier 1純粋関数の実装diff** だけである。
> 代わりに人間が読むのは **仕様（Requires/Ensures）とTCB** である。
> 機械は「仕様が全入力で成り立つこと」をZ3で検査する。
> この分業が成り立つ理由を以下で段階的に示す。

## 1. 保証の主張（正確な言明）

`completeTodo` を例にします。証明器が出す `✓ completeTodo proof` は次を意味します。

```text
Todo型の全ての値 todo について:
  completeTodoRequires(todo) が真ならば、
  completeTodo(todo) の出力は completeTodoEnsures(todo, 出力) を満たす。
```

これはテストの「試した例では動いた」ではなく、入力空間全体への**全称命題**です。
全称を主張できるのは、値を一つずつ試すのではなく、入力を**記号（シンボル）のまま**
扱うからです（§3）。

## 2. 証明の問い（なぜ¬Postを投げるのか）

証明器（`tools/verifier/verify.mjs` の `proveTarget`）は、Z3に次の式を投げます。

```text
Pre(input) ∧ output = Impl(input) ∧ ¬Post(input, output)
```

読み下すと「前提を満たすのに、実装の出力が仕様を**破る**ような入力は存在するか」です。

- 答えが **UNSAT（充足不能）** → そのような入力は存在しない → 仕様は全入力で成立 → 証明成功。
- 答えが **SAT（充足可能）** → 仕様を破る入力が存在する → その入力と出力の具体値を
  モデルから取り出し、counterexampleとして表示 → 証明失敗。

背理法と同じ構造です。「反例の不存在」をソルバーに判定させることで、全称命題を
有限時間で決定しています。これがテスト（存在命題の確認＝「動く例がある」）との本質的な差です。

## 3. なぜ「全入力」について言えるのか（記号的扱い）

証明器は `input.title` に `"a"` や `"test"` といった具体値を入れません。
代わりに `String.const("real__in__title")` という**文字列変数**を作り、
「どんな文字列が来てもよい」ものとして制約だけを載せます（`length >= 1` など）。
Z3の文字列理論の決定手続きは、この変数の取りうる**全ての値**に対して
`output.title == input.title` が成り立つかを判定します。等式の両辺が同じ変数を
指していれば、値が何であれ等しい——この推論を機械が行うため、無限の入力空間を
列挙せずに全称が言えます。

## 4. 翻訳の対応表（実装とZ3のつながり）

証明器は実装の**実際のソース**をTypeScriptコンパイラAPIでパースし
（`parseModule`＋2ファイル結合 `parseProgram`）、次の対応でZ3の項に翻訳します（`ev`）。
対象は `src/verified/specs/todo.ts`（型＋仕様）と `src/verified/impl/todo.ts`
（関数本体）の結合プログラムです。両ファイル間の同名再定義は重複エラーで落とすため、
impl側が仕様をすり替えることは構造的にできません。

| TypeScript（Verifiedサブセット） | Z3 |
|---|---|
| `string` 変数・フィールド | String変数（`String.const`） |
| `boolean` 変数・フィールド | Bool変数（`Bool.const`） |
| 文字列リテラル `"..."`／`true`／整数 | `String.val`／`Bool.val`／`Int.val` |
| `s.length` | `s.length()`（Int項） |
| `===`／`!==` | `Eq`／`Not(Eq)`（ソート一致を要求） |
| `&&`／`||`／`!` | `And`／`Or`／`Not` |
| `< <= > >=`、`+ -`（Int上） | `.lt/.le/.gt/.ge`、`.add/.sub` |
| `{ id: input.id, ..., completed: true }` | フィールドごとの項の束（出力の各欄＝実装の式） |
| `a ? b : c` | `If` |

重要なのは、出力の各フィールドが**実装の式そのものの翻訳**に束縛される点です。
`output` という独立変数を置いて実装との等式を仮定するのではなく、
postcondition中の `output.title` は実装が返す式の翻訳に直接置換されます。
したがって「モデルと実装の食い違いが証明をすり抜ける」余地はありません。

## 5. fail-closed（見逃しを構造的に防ぐ）

翻訳器が知らないASTノード（関数呼び出し、ループ、`as`、`await`等）に当たったら、
その場で `VerifyError` を投げて検証全体を失敗させます。**対応表にないものは
「通す」ことも「黙って無視する」こともありません。** これにより、AIがサブセット外の
コードを生成しても「検証済み」に紛れ込むことはありません。サブセット検査器
（`tools/checks/subset.mjs`）が事前に構文面で弾き、証明器が意味論面で二重に
弾く二層構造です。

## 6. 信頼の連鎖（何を信頼しているか）

証明の正しさは何もないところからは生まれません。信頼しているものを列挙します。
これらは `README.md` の非保証と対応しています。

1. **仕様の正しさ**: `completeTodoEnsures` が「完了とは何か」を正しく形式化していること。
   これは人間がレビューします。機械は「仕様通りか」しか言えません。
2. **翻訳の忠実性**: 上の対応表の実装（`ev` 約120行）が正しいこと。TCBとして人手レビュー対象です。
3. **検査器の正しさ**: サブセット検査・境界検査が違反を見逃さないこと。
   `selftest` が注入違反で検査器をテストし、盲目化を検出します。
4. **Z3の正しさ**: `z3-solver`（Z3 WASM）の判定が正しいこと。Microsoft ResearchのZ3を信頼します。
5. **パースの正しさ**: `tsc` のASTがソースを正しく表すこと。
6. **Portの仮定**: `save` が渡されたものを保存すること等（Tier 2）。証明対象外の明示的仮定です。
7. **バリデータ対応**: zodの境界値と `Requires` の一致（目視で信頼）。

連鎖の要点: 1と6と7とTCBは人間が見る。2〜5は小さく安定した共通部品であり、
**アプリケーションの機能追加では変わりません**。だから機能コードのdiffは見なくてよくなります。

## 7. 具体例でたどる（正常証明と反例）

正常系 `completeTodo`（`{ id: todo.id, title: todo.title, completed: true }`）:

```text
Pre:  todo.completed == false
Post: output.id == input.id ∧ output.title == input.title ∧ output.completed == true
問い: Pre ∧ (output各欄=実装式) ∧ ¬Post … UNSAT → ✓
```

`¬Post` は「idが違うか、titleが違うか、completedが真でない」の選言です。
実装式を代入すると `todo.id == todo.id` 等は恒真に潰れ、残るのは
`¬(false == false ∧ …)` の形の矛盾のみとなり、Z3は即座にUNSATを返します。

異常系 `fixtures/invalid/completeTodo-wipes-title.ts`
（`{ id: todo.id, title: "", completed: true }`）:

```text
問い: Pre ∧ (output.title = "") ∧ ¬Post … SAT
反例: input.title = "A", output.title = ""
```

`completed == true` だけ見る素朴な検査なら通りますが、保存条項
`output.title == input.title` が `¬Post` 側で `"A" == ""` の反例を要求し、
Z3が具体値 `"A"` を持つモデルを返して拒否します。これが「保存されていることの証明」
の実物です。

## 8. 「人間が見なくてよい」の正確な範囲

| 変更対象 | 人間のレビュー | 機械の検査 |
|---|---|---|
| Tier 1実装（`impl/todo.ts` の関数本体） | 不要（証明が通れば） | Z3証明＋サブセット＋境界 |
| 仕様（`specs/todo.ts` のRequires／Ensures） | **必要**（何を保証すべきかの判断） | 構文検査のみ |
| Tier 2（usecase・Port） | 差分の目視推奨（テストが主防御） | 型・境界・単体テスト |
| TCB（`src/tcb/**`、`tools/**`） | **必要**（小さいことが前提） | 型・テスト |
| 依存・基盤（Node、PG、Z3） | 不要（信頼） | なし |

「見なくてよい」は無条件ではありません。正確には **「実装の正しさの確認を
仕様の確認に繰り上げられる」** ということです。人間は「何が成り立つべきか」
（短く安定）に集中し、「成り立っているか」（長く変化する）は機械に任せます。

## 9. 残るリスクと封じ込め

- **仕様が間違っている**: 証明は仕様に対する相対的正しさしか言いません。
  封じ込め: 仕様は短い（本PoCでは各3連言）ためレビューが容易。仕様自体もサブセット内に書かせ、
  複雑な仕様は書けない構造にしています。
- **翻訳器のバグ**: 証明器自体がTCBです。封じ込め: 翻訳器は対応表の素直な実装に限定し、
  ネガティブコントロール（`fixtures/invalid/*` の拒否）が回帰シグナルになります。
- **Port・バリデータの仮定**: 証明外です。封じ込め: アダプタは分岐なしの素直な対応付けに限定し、
  読み出し行はzodで再検証しています。
- **ソルバー・基盤のバグ**: 受容します（形式手法一般の前提）。

## 10. AIループへの接続

`verify` の失敗出力は「合否＋具体的な入出力 counterexample」という機械可読な信号です。
これはそのまま生成AIへの修復プロンプト材料になります
（「`input.title="A"` に対し `output.title=""` になった。保存条項を満たすよう修正せよ」）。
本PoCはこのループの**検証側インターフェース**を既に備えています。
生成側の接続は次の段階の作業です。
